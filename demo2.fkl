(defmacro (let ((ATOM#NAME ANY#VALUE) VAREPT#OTHER),CELL#BODY)
          (cons (cons 'lambda (cons (cons NAME OTHER#NAME) BODY)) (cons VALUE OTHER#VALUE)))
(let ((a 8)
	  (b 9)
	  (c 10)
	  (d 11))
 (print a)
 (print b)
 (print c)
 (print d)
)
