(import btk)
(load macro.fkl)

(if (le (length (argv)) 1)
  (begin
    (princ "No input file.\n" stdout)
    (exit 1)))

(define code
  (let* ((fp (open (nth 1 (argv))))
         (c (read fp)))
  (close fp)
  (str c)))

(define m #b00)

(let iter ((cp 0)
           (mp 0)
           (e #b00))
  ())
