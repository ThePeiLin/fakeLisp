(define id (go (lambda (x)
                 (cond ((eqn (accept) "I'm your father!")
                        (princ "Thread#1:Noooooooo!\n" stdout)
                        (send nil x)
                       )))
               (cons (getid) nil)))
(princ "Thread#0:I'm your father!\n" stdout)
(send "I'm your father!" id)
(accept)
