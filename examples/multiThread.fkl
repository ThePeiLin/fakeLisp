(import (std))
(define ch1 (chanl 0))
(define ch2 (chanl 0))

(go (lambda ()
      (cond ((eqn (recv ch1) "I'm your father!")
             (princ "Thread#1:Noooooooo!\n" stdout)
             (send nil ch2)
             ))))

(princ "Thread#0:I'm your father!\n" stdout)
(send "I'm your father!" ch1)
(recv ch2)
