(define ch #=0)

(go (lambda (ch)
      (cond ((eqn (recv ch) "I'm your father!")
             (princ "Thread#1:Noooooooo!\n" stdout)
             (send nil ch)
             )))
    ch)
(princ "Thread#0:I'm your father!\n" stdout)
(send "I'm your father!" ch)
(recv ch)
