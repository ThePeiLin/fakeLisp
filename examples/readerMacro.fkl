(defmacro "'[a]" (cons (quote quote) (cons a nil)))
(defmacro "\\[a].[b]"
  ((lambda ()
     (define list (lambda ls ls))
     (list 'lambda (cons a nil) b))))

(princ (\x.(add x 9) 9) stdout)
(princ #\\n stdout)
