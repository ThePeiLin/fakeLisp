(load macro.fkl)

(let ((yin
        ((lambda (cc) (princ #\@ stdout) cc)
         (clcc (lambda (c) c)))))
  (let ((yang
          ((lambda (cc) (princ #\* stdout) cc)
           (clcc (lambda (c) c)))))
    (yin yang)))
