(check (eq *platform* "win32")
  (begin
    (import (lib os))
    (system "chcp 65001")))

(import (ascii-art)
        (bf)
        (threading)
        (idle)
        (atexit)
        (demo1)
        (demo2)
        (fib)
        (hello)
        (internationale)
        (test-loc)
        (lex)
        (reader-macro)
        (compiler-macro)
        (test)
        (test-alias)
        (test-only)
        (test-prefix)
        (test-except)
        (big-int)
        (parse)
        (read)
        (bigloop)
        (dll)
        (complex-macro)
        (compile-check)
        (test-ht)
        (os)
        (fs)
        (sync)
        (cir-ref)
        (remq)
        (fuv))

(println "\ntest done!")
