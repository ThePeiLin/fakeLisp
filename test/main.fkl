(import (ascii-art))
(import (bf))
(import (bigloop))
(import (demo1))
(import (demo2))
(import (fib))
(import (hello))
(import (internationale))
(import (test-loc))
(import (readerMacro))

(newline)
(println "test done!")
