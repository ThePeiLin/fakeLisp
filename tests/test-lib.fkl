(export
  (defmacro #&custom
    #[custom-quote #["#quote'" *s-exp*]
                   custom
                   (list 'quote $1)]))

(export
  (defmacro #&comment
    #& #[#&#r"/"/\*\*/|/\*.*\*/"/"]
    "/*"))

(export
  (defmacro test-replacement 'foobar))
